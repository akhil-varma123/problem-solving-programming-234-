#include<stdio.h>
int max()
{
    int a,b;
    printf("enter two numbers to check max:");
    scanf("%d%d",&a,&b);
    if(a>b)
    printf("%d is the max number",a);
    else
    printf("%d is the max number",b);
   
}

int main()
{
    max();
    
}